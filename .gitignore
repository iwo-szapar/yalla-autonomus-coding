# Per-project config the user fills in (the template ships as YALLA.example.md)
.claude/YALLA.md

# Pipeline runtime state and evidence artifacts
.pipeline/
.pipeline-state.json
plans/
.claude/worktrees/

# Eval harness / Node deps and build output
node_modules/
dist/

# OS / editor noise
.DS_Store
*.swp
.idea/
branch_structure.json
temp_auto_push.bat
temp_interactive_push.bat
.gitignore
